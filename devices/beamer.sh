#/bin/bash

acroread -geometry 1024x768+1920+0 "$1"

# Actually to make it full screen, drag it to the beamer screen, make it such that it is half-full screen (it occupies the left part of the beamer screen). This can be done by dragging it to the bottom. And only then make it fullscreen.
